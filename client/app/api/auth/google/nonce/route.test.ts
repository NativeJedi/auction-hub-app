import { describe, it } from 'vitest';

describe('GET /api/auth/google/nonce', () => {
  it.todo('proxies the Nest response and returns { nonce }');
  it.todo('sets no cookies on the response');
  it.todo('forwards Nest error status and body verbatim');
});
