import { describe, it } from 'vitest';

// Scaffold — filled in by sdlc-tests.
// robots() is a pure function returning a MetadataRoute.Robots object.
describe('robots', () => {
  it.todo('allows the "*" user agent to crawl "/"');
  it.todo('references the sitemap URL');
});
