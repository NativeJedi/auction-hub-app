// @vitest-environment jsdom
import { describe, it } from 'vitest';

// Scaffold — filled in by sdlc-tests.
// Hero renders two CTAs: a primary one targeting registration and a secondary
// one targeting login.
describe('Hero', () => {
  it.todo('primary CTA links to the registration screen (/crm/auth?type=register) — AC-3');
  it.todo('secondary CTA links to the login screen (/crm/auth) — AC-3');
});
