import { describe, it } from 'vitest';

describe('getRoomToken', () => {
  it.todo('returns value stored under room:${auctionId}:token');
});

describe('setRoomToken', () => {
  it.todo('writes value to localStorage under room:${auctionId}:token');
});
